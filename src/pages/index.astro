---
import { Home } from "~/components/organisms/Home";
import MainLayout from "~/layouts/MainLayout.astro";
---

<MainLayout title="Home">
	<Home client:load />
	{["ONE", "TWO", "THREE"].map(text => (
	/* temporary dummy stuff */
	<div
		style="height: 100vh; font-size: 6.4rem; display: flex; align-items: center; justify-content: center; font-family: 'Raleway', sans-serif; font-weight: 800; scroll-snap-align: center">
		{text}
	</div>
	))}

</MainLayout>
